version: '3'

services:
  app:
    build: .
    ports:
      - "8080:8080"

  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx_reverse_proxy.conf.j2:/etc/nginx/conf.d/default.conf
    environment:
      - SERVER_NAME=${SERVER_NAME}
    depends_on:
      - app