server {
    listen 80;
    server_name ${SERVER_NAME};

    location / {
        proxy_pass http://furkan-app:4040;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
